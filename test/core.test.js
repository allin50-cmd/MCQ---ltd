import test from "node:test";import assert from "node:assert/strict";import {isAvailable,createQuote,confirmBooking} from "../src/core.js";
test("real-price quote uses integer pence and 25% deposit",()=>{const q=createQuote({id:"e1"},{id:"eq1",price_pence:10000});assert.equal(q.total_pence,10000);assert.equal(q.deposit_pence,2500)});
test("confirmed booking blocks overlapping hire",()=>{const bookings=[];const quote={equipment_id:"eq1",total_pence:10000,deposit_pence:2500};const payment={status:"RECEIVED",amount_pence:2500};bookings.push(confirmBooking({quote,payment,start_at:"2026-12-20T10:00:00Z",end_at:"2026-12-21T10:00:00Z",bookings}));assert.equal(isAvailable("eq1","2026-12-20T12:00:00Z","2026-12-20T18:00:00Z",bookings),false);assert.throws(()=>confirmBooking({quote,payment,start_at:"2026-12-20T12:00:00Z",end_at:"2026-12-20T18:00:00Z",bookings}),/unavailable/)});
test("status flag alone cannot confirm without received payment",()=>assert.throws(()=>confirmBooking({quote:{equipment_id:"eq1",total_pence:10000,deposit_pence:2500},payment:{status:"PENDING",amount_pence:2500},start_at:"2026-12-20",end_at:"2026-12-21",bookings:[]}),/deposit not received/));
