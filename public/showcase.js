const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
async function api(url,opt={}){const r=await fetch(url,{...opt,headers:{"content-type":"application/json",...(opt.headers||{})}});const t=await r.text();let d;try{d=t?JSON.parse(t):null}catch{d=t}if(!r.ok){const e=new Error(d?.error||d?.message||`Request failed (${r.status})`);e.data=d;throw e}return d}
const data=window.MCQ_SHOWCASE;
function productCard(p){return `<article class="card"><div class="card-media"><img loading="lazy" src="${esc(p.image)}" alt="${esc(p.brand+" "+p.name)}" onerror="this.style.opacity=.12"></div><div class="card-body"><div class="brand">${esc(p.brand)} / ${esc(p.use)}</div><h3>${esc(p.name)}</h3><div class="price">${esc(p.price)}</div><p>${esc(p.desc)}</p><div class="specs">${p.specs.map(s=>`<span>${esc(s)}</span>`).join("")}</div><div class="actions"><a class="btn" href="${esc(p.url)}" target="_blank" rel="noreferrer">View product</a><button class="btn alt source" data-query="${esc(p.query)}">Find / buy</button></div></div></article>`}
for(const tier of data.tiers){const el=document.querySelector(`[data-tier="${tier.id}"] .product-rail`);if(el)el.innerHTML=tier.products.map(productCard).join("")}
$$(".source").forEach(b=>b.addEventListener("click",()=>{document.querySelector("#shop-search").value=b.dataset.query;document.querySelector("#shop").scrollIntoView({behavior:"smooth"});setTimeout(()=>runSearch(b.dataset.query),300)}));
let suppliers=[];async function load(){try{suppliers=await api("/api/suppliers");$("#supplier-select").innerHTML=suppliers.map(s=>`<option value="${esc(s.id)}">${esc(s.name)}</option>`).join("")}catch{$("#supplier-select").innerHTML="<option>Supplier</option>"}}function link(s,q){return s.search_url.replace("{query}",encodeURIComponent(q))}
async function runSearch(q){q=String(q||$("#shop-search").value||"").trim();if(!q)return;const out=$("#live-products");out.innerHTML='<div class="message">Checking current supplier route…</div>';const id=$("#supplier-select").value||"farnell",s=suppliers.find(x=>x.id===id)||suppliers[0];if(id==="farnell"){try{const d=await api(`/api/suppliers/farnell/search?q=${encodeURIComponent(q)}`);if(d.products?.length){out.innerHTML=d.products.map(p=>`<div class="live-row">${p.image_url?`<img src="${esc(p.image_url)}" alt="">`:"<div></div>"}<div><b>${esc(p.name||p.sku)}</b><br><small>${esc(p.brand||"Farnell")} • ${esc(p.sku||"")}</small></div>${p.product_url?`<a class="btn" href="${esc(p.product_url)}" target="_blank" rel="noreferrer">View / buy</a>`:""}</div>`).join("");return}}catch{}}const u=s?link(s,q):"https://uk.farnell.com/search?st="+encodeURIComponent(q);out.innerHTML=`<div class="message"><b>${esc(s?.name||"Supplier")}</b><p>Open current supplier stock and pricing.</p><a class="btn" href="${esc(u)}" target="_blank" rel="noreferrer">Open live search</a></div>`}
$("#shop-search-btn").addEventListener("click",()=>runSearch());$("#shop-search").addEventListener("keydown",e=>{if(e.key==="Enter")runSearch()});load();